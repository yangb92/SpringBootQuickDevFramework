<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yangb.project.quickdev.dao.AppPermissionMapper">
  <resultMap id="BaseResultMap" type="com.yangb.project.quickdev.entity.AppPermission">
    <!--
      WARNING - @mbg.generated
    -->
    <id column="id" jdbcType="INTEGER" property="id" />
    <result column="parent_id" jdbcType="INTEGER" property="parentId" />
    <result column="permission_code" jdbcType="VARCHAR" property="permissionCode" />
    <result column="name" jdbcType="VARCHAR" property="name" />
    <result column="url" jdbcType="VARCHAR" property="url" />
    <result column="icon" jdbcType="VARCHAR" property="icon" />
    <result column="permission_order" jdbcType="INTEGER" property="permissionOrder" />
    <result column="permission_type" jdbcType="INTEGER" property="permissionType" />
  </resultMap>

    <select id="queryByRoleId" parameterType="int" resultMap="BaseResultMap">
      SELECT * FROM app_permission WHERE id IN (SELECT permission_id FROM app_role_permission WHERE role_id=#{roleId})
    </select>

    <select id="queryByUserid" parameterType="int" resultMap="BaseResultMap">
    SELECT
        *
    FROM
        app_permission
    WHERE
        id IN (
            SELECT
                permission_id
            FROM
                app_role_permission
            WHERE
                role_id IN (
                    SELECT
                        role_id
                    FROM
                        app_user_role
                    WHERE
                        user_id = #{userid}
                )
        )

  </select>
    <select id="queryUserMenu" resultMap="BaseResultMap">
    SELECT
        *
    FROM
        app_permission
    WHERE
        id IN (
            SELECT
                permission_id
            FROM
                app_role_permission
            WHERE
                role_id IN (
                    SELECT
                        role_id
                    FROM
                        app_user_role
                    WHERE
                        user_id = #{userid}
                )
        )
        AND  permission_type = 1
    </select>
</mapper>