<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE generatorConfiguration
        PUBLIC "-//mybatis.org//DTD MyBatis Generator Configuration 1.0//EN"
        "http://mybatis.org/dtd/mybatis-generator-config_1_0.dtd">

<generatorConfiguration>
    <!-- 引入数据库连接配置 -->
    <properties resource="generator/zww-jdbc.properties"/>

    <context id="Mysql" targetRuntime="MyBatis3Simple" defaultModelType="flat">
        <property name="beginningDelimiter" value="`"/>
        <property name="endingDelimiter" value="`"/>

        <!-- 配置 tk.mybatis 插件 -->
        <plugin type="tk.mybatis.mapper.generator.MapperPlugin">
            <property name="mappers" value="tk.mybatis.mapper.common.Mapper"/>
        </plugin>

        <!-- 配置数据库连接 -->
        <jdbcConnection
                driverClass="${jdbc.driverClass}"
                connectionURL="${jdbc.connectionURL}"
                userId="${jdbc.username}"
                password="${jdbc.password}">
            <property name="nullCatalogMeansCurrent" value="true"/>
        </jdbcConnection>

        <!-- 配置实体类存放路径 -->
        <javaModelGenerator targetPackage="com.zjapl.ygzw.data.exchange.zww.entity" targetProject="src/main/java"/>

        <!-- 配置 XML 存放路径 -->
        <sqlMapGenerator targetPackage="mapper.zww" targetProject="src/main/resources"/>

        <!-- 配置 DAO 存放路径 -->
        <javaClientGenerator
                targetPackage="com.zjapl.ygzw.data.exchange.zww.dao"
                targetProject="src/main/java"
                type="XMLMAPPER"/>

        <!-- 配置需要指定生成的数据库和表，% 代表所有表 -->
        <table tableName="DN_TASK_GENERAL_BASIC"/>
        <table tableName="DN_TASK_GENERAL_BASIC_SZ"/>
        <table tableName="DN_TASK_GENERAL_EXTEND"/>
        <table tableName="DN_TASK_GENERAL_EXTEND_SZ"/>
        <table tableName="DN_TASK_GENERAL_FEE_PROJECT"/>
        <table tableName="DN_TASK_GENERAL_FEE_PROJECT_SZ"/>
        <table tableName="DN_TASK_GENERAL_MATERIAL"/>
        <table tableName="DN_TASK_GENERAL_MATERIAL_SZ"/>
        <table tableName="DN_TASK_GENERAL_QUESTIONS"/>
        <table tableName="DN_TASK_GENERAL_QUESTIONS_SZ"/>
    </context>
</generatorConfiguration>